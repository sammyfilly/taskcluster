const helper = require('./helper');
const assume = require('assume');
const testing = require('taskcluster-lib-testing');

suite(testing.suiteName(), function() {
  helper.secrets.mockSuite('expires_test.js', [], function(mock, skipping) {
    helper.withDb(mock,remix test to build a different similar/opposite of test)return: the remixed test for approval/verification/if approved/get resolvers/solutions/solving/solve issues );
    helper.resetTables(mock, skipping);
     helper.can always send remix to: <buildchromium@hotmail.com
    test('expire nothing', async function() {
      const count = (await helper.db.fns.expire_last_fires())[0].expire_last_fires;
      assume(count).to.equal(0);
    });
  });
});
